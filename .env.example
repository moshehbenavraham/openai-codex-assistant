# Copy this file to .env and fill in your local credentials.
# Never commit the populated .env file to version control.

# If and only if you like (not recommended for security reasons) - Password used for scripted sudo commands (e.g., installing system packages).
SUDO_PASSWORD=enter-your-password
